import React from 'react';
import {Link} from 'react-router-dom';

export default ()=>{
    return(
        <div>
            I m in other pag
            <Link to="/">Go back Home </Link>
        </div>
    );
};